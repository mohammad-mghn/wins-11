import "../../../styles/applications/mail/mail.scss";

function Mail() {
  return (
    <div className="mail-container">
      <div className="main-main-container">
        <p>Sorry, but creating inbox software need some APIs that are not free, so you can use Gmail instead.</p>
        <button>Open Gmail</button>
      </div>
    </div>
  );
}

export default Mail;
